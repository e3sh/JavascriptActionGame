BASE TASK SYSTEM

gmain = new GameCore(paramater)

gmain.state.dev
gmain.state.system
gmain.state.game
gmain.state.config
gmain.state.result
gmain.asset



SystemArchParamator
resolution
layer
mouse
keyboard
vkey


GameStageParamater
geometory

AssetParamater
imagefile
soundfile
etcfile(json)
mapdata charactordata mp spdata


gmain.task

gmain.task.init  //syoki
gmain.task.pre   //stepmaeno pre
gmain.task.step
gmain.task.draw
gmain.task.post

task.enable
task.visible


gmain.settask return taskhandle
gmain.deletetask return state



gmain.pause
gmain.run


runでゲームループを開始
gmain生成時点でシステムとしてはループを
開始してメッセージループを
回す

task.pause

device_paramater(emu system design)

gamesystem_logic

------------------------------------------
	      - sp_Setpp(img, sp_array)
SpriteControl - motion_setup(number, motionArray)
scrn.Sprite.move( num. x, y )
scrn.Sprite[ num ].visible = true 
scrn.useSprite = true;

--------------------
scrn.useBG_PCG = ?
BG_PCG - font.cursor(x,y) ;font.screen( screen );
(FontControl) - wprint (screen, string , x, y ); print (string)

bgput(num.x,y)
bg_Stpp(img, bg_array)

Screen SpriteControl SetImg SpImG    OffScreenBuffer => Canvas
       FontControl SetImg CodeList
       BGControl SetImg aList
       SpAnimation

task_system


loop


taskbase

template



処理の見通しをよくする。↓別タスク化
ゲームループ/表示　loop1

文字表示　loop2

その他処理　タイトルなど　loop3

随意に処理させる　loop4



